apiVersion: apps/v1
kind: Deployment
metadata:
  name: swish-analytics-demo
  labels:
    app: r-python-alpine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: r-python-alpine
  template:
    metadata:
      labels:
        app: r-python-alpine
    spec:
      containers:
      - name: r-python-alpine
        image: ellipticalgadzooks/python_r_alpine:latest
        command: ["tail"]
        args: ["-f", "/dev/null"]
